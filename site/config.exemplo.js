// Copie este arquivo para config.js e preencha com os dados do SEU projeto
// Supabase (Project Settings › API).
//
// Estas duas informações são públicas por natureza — qualquer visitante do site
// consegue lê-las, e tudo bem: quem protege os dados são as políticas de
// segurança do banco, não o segredo da chave. A chave que NUNCA pode aparecer
// aqui nem em lugar nenhum do site é a "service_role": ela ignora todas as
// políticas. Se um dia ela vazar, gere outra imediatamente no painel.

export const CONFIG = {
  supabaseUrl: "https://SEU-PROJETO.supabase.co",
  supabaseAnonKey: "COLE-AQUI-A-CHAVE-ANON-PUBLIC"
};
